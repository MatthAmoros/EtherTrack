language: node_js
node_js:
 - "node"
install:
 - npm install -g firebase-tools
script:
 - npm test
 jobs:
  include:
    - stage: firebase deploy
       deploy:
        provider: firebase
        token:
         secure: FIREBASE_TOKEN
