{
  "rules": {
    "appParameters" : {
      ".write" : false,
      ".read" : true
    },    
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    }
  }
}
